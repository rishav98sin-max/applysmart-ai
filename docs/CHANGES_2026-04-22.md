# Changes Made - April 22, 2026

## 1. Semantic Matching Fix (job_matcher.py)
**File:** `agents/job_matcher.py` (line 388)

**Change:** Updated semantic matching rules to include explicit mappings for:
- "agent-based AI systems orchestration"
- "automation workflows"
- "APIs"
- "SaaS tools"
- "third-party machine learning products"

**Reason:** These skills were being incorrectly marked as "missing" even though the user's CV contains related terms like "AI", "LangGraph", "APIs", etc.

---

## 2. UI Stage Indicators
**Files:** `agents/job_agent.py`, `app.py`

**Changes:**
- Added `progress_callback` parameter to `run_agent()` function
- Added `progress_callback` field to `AgentState` TypedDict
- Added `_report_progress()` helper function to safely call progress callback
- Added `_check_stop_requested()` helper function to check for stop requests
- Added progress calls to all worker nodes:
  - `validate_inputs_node` → "Validating inputs"
  - `parse_cv_node` → "Parsing CV"
  - `extract_cv_style_node` → "Extracting CV style"
  - `planner_node` → "Planning search strategy"
  - `scrape_jobs_node` → "Scraping job boards (round N)"
  - `match_jobs_node` → "Matching jobs to CV"
  - `tailor_and_generate_node` → "Tailoring CVs and generating cover letters (N jobs)"
  - `send_email_node` → "Sending email with documents"
- Moved stage indicator outside `st.status` block in `app.py` for better visibility
- Stage displays as "**Current Stage:** [stage name]"

**Reason:** Users couldn't see what stage the agent was at during execution, leading to poor feedback.

---

## 3. Stop Button
**Files:** `agents/job_agent.py`, `app.py`

**Changes:**
- Added "⏹ Stop" button in `app.py` above the status box
- Button sets `st.session_state["_stop_requested"] = True`
- Added stop check in `supervisor_node()` in `job_agent.py`
- If stop requested, agent exits with "halted_user_stop" status
- Added "halted_user_stop" to `HARD_TERMINAL_STATUSES`

**Reason:** Users needed a way to cancel the agent if they made a mistake with job inputs, to save tokens.

---

## 4. LinkedIn Description Retry Logic
**File:** `agents/job_scraper.py` (function `_fetch_linkedin_description`)

**Change:** Added retry logic that:
- Attempts to fetch description twice
- Retries with 2-second delay if first attempt returns empty or <30 chars
- Logs retry attempts

**Reason:** Intermittent network issues or LinkedIn rate limiting were causing empty descriptions, leading to valid jobs being skipped.

---

## 5. Gemini API Key Debug Logging
**File:** `agents/llm_client.py` (function `_call_gemini`)

**Change:** Added debug logging that:
- Logs the length of the API key
- Logs the first 10 characters of the key
- Warns if key doesn't start with "AIza"

**Reason:** Gemini API key was showing "API_KEY_INVALID" error despite being set in secrets. Debug logging helps identify if the key is being loaded correctly.

---

## Git Commit
**Commit:** `38302a3` - "Add UI stage indicators, stop button, LinkedIn description retry, and Gemini key debug logging"
**Files changed:** 7 files, 158 insertions(+), 34 deletions(-)

---

## Pending Checks for Tomorrow

### High Priority
1. **Check Gemini API key debug logs** - After redeploy, check logs for "🔑 Gemini API key loaded" line to see key length and prefix
2. **Verify Gemini API key validity** - Ensure key starts with "AIza" and is a valid Google AI Studio key
3. **Test Gemini writing tasks** - Verify Gemini is used for cover letters and CV tailoring instead of falling back to Groq

### Medium Priority
4. **Test stage indicator** - Verify current agent stage displays correctly during execution
5. **Test stop button** - Verify stop button cancels agent execution and saves tokens
6. **Test description retry** - Verify LinkedIn description retry reduces "empty description" skips

---

## Additional Fixes (April 22, 2026 - Evening)

### 7. ChromaDB v0.6.0 Compatibility Fix
**File:** `agents/cv_embeddings.py` (line 165-166)

**Change:** Updated `list_collections()` call to handle Chroma v0.6.0 breaking change:
- Old: `existing = {c.name for c in client.list_collections()}` (expected collection objects)
- New: `existing = set(client.list_collections())` (handles string names only)

**Reason:** Chroma v0.6.0 changed `list_collections()` to return only collection names (strings) instead of full collection objects, causing `NotImplementedError`. This broke RAG vector indexing, causing fallback to full CV text instead of chunked retrieval.

---

### 8. PDF Bullet Character Extraction Fix
**File:** `agents/cv_parser.py` (function `parse_cv`)

**Change:** Changed PDF text extraction from default mode to dict mode:
- Old: `page.get_text()` (default mode)
- New: `page.get_text("dict")` with span-level extraction

**Reason:** Default PyMuPDF extraction was converting special bullet characters (→, ▸, •) to ? symbols due to encoding issues. The dict mode preserves character encoding better by extracting text at the span level, preventing garbled bullet characters in the parsed CV text.

**Impact:** This fixes the issue where tailored CVs had ? symbols instead of proper bullets because the LLM was working with broken source text from the PDF parser.

---

## Known Issues
- **Gemini API Key:** Previously showing "API_KEY_INVALID" error. Debug logging added. Key was regenerated and should now work. Verify in next run.

---

## Afternoon / Evening Batch (April 22, 2026)

### 9. WeasyPrint HTML/CSS rebuild path
**Files:** `agents/pdf_formatter_weasy.py` (new), `agents/templates/cv_modern.html` (new), `agents/templates/cover_letter_modern.html` (new), `agents/pdf_formatter.py`, `packages.txt` (new), `requirements.txt`

**Change:** Added a new rebuild renderer based on WeasyPrint + Jinja2 HTML templates. `generate_cv_pdf_styled` now tries WeasyPrint first; only falls back to the legacy ReportLab path when WeasyPrint fails. Auto-scales typography to preserve the original page count (1-page CV stays 1 page). Added `packages.txt` to install the native deps (`libpango`, `libcairo`, etc.) on Streamlit Cloud.

**Reason:** Designer-template CVs (Novoresume, Canva) were falling through to ReportLab, which produced ATS-unfriendly output — no summary section, bullets rendered as `-`, missing contact details, wrong page count. The HTML/CSS path renders a clean, single-column, ATS-safe layout with proper bullets and a visible contact strip.

### 10. Canonical CV section order
**Files:** `agents/pdf_formatter.py`, `agents/pdf_formatter_weasy.py`

**Change:** Both renderers now sort sections into a fixed order before drawing: **Header → Professional Summary → Experience → Projects → Education → Skills → Certifications / Other**. The order is pure-Python, so it applies regardless of which LLM produced the tailored outline.

**Reason:** Explicit user requirement. Also insulates layout from LLM variance — Groq-fallback outputs occasionally shuffled sections.

### 11. Summary prompt rewritten for 6-second recruiter scan
**File:** `agents/cv_diff_tailor.py`

**Change:**
- Length cap tightened — tailored summary may shrink no more than **5%** vs original.
- Framing emphasises "6-second recruiter scan" + JD-relevant skill highlighting.
- Fabrication ban made explicit with concrete examples.
- Post-generation guard reverts summary to original when CV-foreign proper nouns are detected, or when length falls below the 95% floor.
- Skills reordering **disabled** in both prompt and sanitiser — the original skills list is preserved verbatim.

**Reason:** Previous prompt shortened summaries aggressively and occasionally inserted JD-only tools into them; user wanted minimal, impactful edits grounded only in the CV.

### 12. Cover letter fabrication guard (prompt + post-gen)
**File:** `agents/cover_letter_generator.py`

**Change:**
- Added a "HARD FABRICATION BAN (ZERO TOLERANCE)" block to the prompt with concrete Python/JMeter-style examples.
- Added a post-generation guard `_foreign_terms_in_letter()` that scans each draft for capitalised/acronym tokens not present in the CV, company name, or job title. ≥2 foreign tokens → retry with a tightened prompt appended; all retries exhausted → placeholder.

**Reason:** The Apr 22 AM CIBC run produced a cover letter citing Python, VBScript, JMeter, Neo Load, GitHub — none of which were in the candidate's CV. This runs regardless of which LLM produced the draft, so Groq fallback is also protected.

### 13. Bullet-tailoring rules + retries
**File:** `agents/cv_diff_tailor.py`

**Change:** Explicit bullet-rewrite rules per role relevance tier — rewrite ≥50% in highly-relevant roles, ≥30% in moderately-relevant roles, at least 1 bullet per role otherwise. Retry loop capped; scaled token budgets (1100 → 1500 → 1800) for large CVs. Retry verdict preserves the best version so far.

**Reason:** Earlier conservative tailoring produced 93.5% cross-job similarity. New rules force actual tailoring per JD while the sanitiser guards against fabrication.

### 14. Gemini key rotation pool
**File:** `agents/llm_client.py`

**Change:** Mirrors the Groq rotation pattern. Reads `GEMINI_API_KEY`, `GEMINI_API_KEY_2`, `GEMINI_API_KEY_3`. `_gemini_configure_current()` reconfigures the SDK only when the active key changes. On 429 / quota / auth errors, `_rotate_gemini_key()` advances the cursor and retries immediately. When every key is exhausted, falls back to Groq. Fires `llm_rate_limit_hit` Mixpanel event with `provider=gemini` for infra telemetry.

**Reason:** Single Gemini 2.5 Flash key hits daily quota quickly when testing. Three keys triple the daily envelope. Required because dual-LLM architecture depends on Gemini for tailoring quality.

### 15. Deployment-wide usage counter
**Files:** `agents/llm_client.py`, `app.py`

**Change:**
- Quota cache moved to a file in `OUTPUT_DIR` (writable on Streamlit Cloud) so usage persists across process restarts.
- Groq response headers trigger daily-reset detection (jump in remaining-tokens → zero the counter).
- UI: expander replaced with a single-line counter showing runs-left deployment-wide (User A using 2 runs reduces what User B sees). Font size enlarged for readability.

**Reason:** Users were seeing inconsistent counters across browsers/tabs because tracking was in-memory per-process. File-based cache + header reconciliation gives one truthful number.

### 16. Mixpanel distinct_id persistence
**File:** `app.py`

**Change:** Anonymous distinct_id now lives in the URL query param `?aid=<uuid>`, so refreshing the page or closing/reopening the tab keeps the same Mixpanel identity. First visit generates + writes the param; subsequent visits read it. Falls back to `session_state` if `st.query_params` is unavailable.

**Reason:** Funnels filtered to "Today" showed `cv_uploaded=11, run_started=0` while un-filtered events confirmed 6 `run_completed` events today. Refreshes during testing created new `distinct_id`s, breaking the funnel chain. Persisting the ID restores correlation across refreshes.

### 17. Streamlit log noise silenced
**File:** `.streamlit/config.toml`

**Change:** Disabled file watcher (`runner.fastReruns=true`, `server.fileWatcherType="none"`), lowered Streamlit internal logger to `error`. Transformers module traceback spam from the watcher is gone. App `print()` output is still visible.

**Reason:** Logs were overwhelmingly filled with transformers/torchvision re-import tracebacks on every rerun, hiding real warnings.

### 18. CV parser bullet glyph fix
**File:** `agents/cv_parser.py`

**Change:** Detects symbol fonts that decode bullets as `?` (unicode U+F0B7, etc.), repairs to proper `•` before handing the text to the LLM, and preserves line breaks from the PDF structure.

**Reason:** Designer-template bullets were arriving at the LLM as literal `?` characters, which polluted the rewritten output.

### 19. Tailor concurrency + stop-check
**File:** `agents/job_agent.py`

**Change:** `tailor_and_generate_node` runs CV-tailor + cover-letter in parallel per job, with a stop-check between steps so "Cancel" exits cleanly without burning the remaining LLM budget.

**Reason:** Sequential tailor-then-cover added ~1.5x latency per job. Concurrent where safe.

### 20. Packages manifest + redeploy
**File:** `packages.txt` (new)

**Change:** Declares apt-packages required by WeasyPrint (`libpango-1.0-0`, `libpangoft2-1.0-0`, `libharfbuzz0b`, `libcairo2`, `libgdk-pixbuf-2.0-0`, `libffi-dev`, `shared-mime-info`, `fonts-liberation`).

**Reason:** Without these, `import weasyprint` fails on Streamlit Cloud and the ReportLab fallback silently takes over.

---

## End-of-day state

**Shipped today:** 20 distinct changes across 8 files, pushed as commits `…` through `2bbcb24`.

**What tomorrow's first run should verify:**
1. Gemini key rotation logs a `🔄 Gemini key rotated → key #2 of 3` message as key #1 hits quota.
2. Generated CV opens with **Name · Title · Contact** header, shows **Professional Summary** first, bullets render as real `•`, and page count matches the original.
3. Cover letter does NOT contain any tool/language not present in the CV (log line `⚠️ Cover letter introduced CV-foreign terms…` if it slips).
4. URL shows `?aid=<uuid>` — refreshing keeps the same value → Mixpanel funnel conversion > 0% on "Today" filter.
5. Deployment usage counter decrements deterministically as users burn runs.
