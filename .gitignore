# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
env/
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Project artefacts
uploads/
outputs/
data/
!data/.gitkeep

# Per-session user data (CVs, generated PDFs) — contains PII, never commit
sessions/

# Runtime state / caches
.langgraph_api/
graphify-out/
.graphify_*
.quota_cache.json

# Test scratch
_probe*.py

# Streamlit secrets (local only — on Streamlit Cloud use the UI instead)
.streamlit/secrets.toml

# Logs
*.log

# Personal CVs / test artefacts that must never ship publicly
*_layout.txt
_*.txt
Rishav_Singh_*
test_pdf_fix_*.pdf

# Debug/error screenshots (not part of source)
Error SS/
*.debug.png

# Diagnostic run artefacts (per-call LLM telemetry — see diagnostics/README.md)
diagnostics/runs/

# Personal CVs folder (real candidate PDFs — never commit)
CVs/

# Temporary scratch / one-off run scripts at repo root
tmp_*.py
tmp_*.txt
tmp_*.docx
tmp_*.pdf
tmp_*/

# LangFuse local log dumps (per-run telemetry; large and PII-bearing)
diagnostics/LangFuseLogsRun*.jsonl
diagnostics/LangFuseRun*.jsonl
diagnostics/Langfuse_*.json
diagnostics/langfuse_*.jsonl
diagnostics/langfuse_*.py
diagnostics/baseline_log*.jsonl
diagnostics/Langfuse_baslineLog*.json

# One-off diagnostic scripts (kept locally, not committed)
diagnostics/_*.py
diagnostics/_*.pdf

# Audit reports (per-session — keep locally)
AUDIT_REPORT_*.md
